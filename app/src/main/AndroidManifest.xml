<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.appresenias">
    <!-- ✅ Detecta cámara, pero no la requiere obligatoriamente -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" /> <!-- ✅ Permisos modernos -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" /> <!-- Compatibilidad con versiones antiguas (solo se aplican hasta el SDK indicado) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <application
        android:name=".MyTestApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppResenias"
        tools:targetApi="34">
        
        <!-- Activities del flujo de Reseñas -->
        <activity
            android:name=".ReseniasActivity"
            android:exported="false" />
        <activity
            android:name=".SubirReseniaActivity"
            android:exported="false" />
        <activity
            android:name=".resenia"
            android:exported="false" /> <!-- Dejada para el futuro (detalle) -->

        <!-- Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AppResenias.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Otras Activities (revisar si siguen siendo necesarias) -->
        <activity
            android:name=".camara"
            android:exported="false" />
        <activity
            android:name=".recetas"
            android:exported="false" />
        <activity
            android:name=".lista_platos"
            android:exported="false" />

        <!-- FileProvider para la cámara -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="androidx.core.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>